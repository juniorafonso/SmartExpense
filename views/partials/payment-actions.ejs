<div class="dropdown">
    <button class="btn btn-sm btn-outline-secondary dropdown-toggle" type="button"
            data-bs-toggle="dropdown" aria-expanded="false">
      <%= lang.actions || 'Actions' %>
    </button>
    <ul class="dropdown-menu">
      <li>
        <a class="dropdown-item" href="#" onclick="abrirModalEditar(<%= payment.id %>)">
            <%= lang.edit || 'Edit' %>
          </a>
      </li>
      <li>
        <form action="/payments/<%= payment.id %>/delete" method="POST" style="display:inline;">
          <button type="submit" class="dropdown-item text-danger delete-payment-button">
            <%= lang.delete || 'Delete' %>
          </button>
        </form>
      </li>
    </ul>
  </div>
  